{
  "name": "balaji-whatsapp-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Reads configured WhatsApp groups and forwards messages to the Balaji CRM ingestion webhook.",
  "main": "src/index.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.js",
    "pair": "node --env-file-if-exists=.env src/index.js --pair",
    "groups": "node --env-file-if-exists=.env src/index.js --list-groups",
    "check": "node --check src/index.js && node --check src/ingest.js && node --check src/config.js && node --check src/backfill.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=22.9"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "^6.7.24",
    "pino": "^9.5.0",
    "pino-pretty": "^13.0.0",
    "qrcode-terminal": "^0.12.0"
  }
}
